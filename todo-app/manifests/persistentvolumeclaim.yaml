apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-claim
  namespace: todo-namespace
spec:
  storageClassName: manual
  volumeName: todo-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
